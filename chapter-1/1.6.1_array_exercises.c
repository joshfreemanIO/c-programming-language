#include <stdio.h>

/**
 * Execute in termial and then type and press enter
 */
void main()
{
    exercise1_13();
}


#define MAX_WORD_LENGTH 20

/**
 * Write a program to print a histogram of the frequencies of
 * different characters in its input.
 */
void exercise1_13()
{
    int c;
    int unique_characters;
    char character_count[], characters_added[];

    while ((c = getchar()) != EOF)
        characters_added = c;
        ++character_count[c];

}
